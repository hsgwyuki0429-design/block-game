# SLIDE POP!

**滑らせて、ぶつけて、盤面を空にする。**

ブロックは押せば壁にぶつかるまで一直線に滑る。同じ色のブロック同士が触れれば弾けて消え、消えた跡には「空きマス」という通路が生まれる。重力も連鎖もない。あるのは **「空きマスこそが唯一の道具」** という事実だけ。

盤面に置かれる色は **ちょうど 2 個ずつ**。だから 1 手で消えるのは必ずその 2 個で、**最短手数＝色数** が構造的に決まる。レベル1から無限に続くレベル制。依存パッケージゼロのバニラ JavaScript（Canvas + WebAudio）製。

```
npm start          # http://localhost:8080 で遊ぶ
npm run build      # src/ から app.js を生成（src/ を編集したら必ず実行）
npm test           # ルール・生成器・レベル進行・配信物のテスト（56 件）
npm run verify     # レベル 1〜80 をすべて生成して検証し、統計を出す
```

`index.html` を `file://` で直接開いても動きます。

---

## ルールは 3 つだけ

| | ルール | 意味 |
|---|---|---|
| ① | **スライド** | できるのは「どのブロックを、どの方向に押すか」だけ。壁か他のブロックにぶつかるまで滑り続ける。**距離は選べない** |
| ② | **同色接触消去** | 滑り終えた直後、上下左右で同色が触れていればその 2 個が消える。列を揃える必要はない。**単体は自己消去しない** |
| ③ | **重力なし** | 消えても他は落ちない。**空きマスは消去でしか生まれず**、生まれた空きマスが次のスライドの通路になる |

- **勝利**：盤面のブロックを全て消す
- **敗北条件は無い** ―― 同じ色は 2 個ずつしか無いので、消せる手が見当たらない局面でも、何も消さない手で通路を作れば必ず解ける。詰まったら `戻す` と `やり直す` がいつでも使える

---

## 3 つの画面

| 画面 | 中身 |
|---|---|
| **ホーム** | 「ゲームスタート」（＝いま挑戦中のレベル）と「レベル一覧」。クリア数・星の総数・到達レベルを表示 |
| **レベル一覧** | 30 レベルずつのページ送り。**クリア済みには取った星が、未開放には錠前が並ぶ** |
| **ゲーム** | 盤面と、手数／PAR／残り、色ごとの残数、下に浮くツールバー |

**ひとつ前のレベルをクリアしないと、次のレベルには行けません。** 進行状況（開放済みレベル・星・自己ベスト）はブラウザに保存されます。

### 星は手数で決まる

| 星 | 条件 |
|---|---|
| ★★★ | **PAR 以内**（保証解と同じかそれ以上に短い） |
| ★★ | PAR + 2 以内 |
| ★ | クリア |

星はレベルごとに最高記録だけが残り、レベル一覧に表示されます。

---

## レベル ― 1 から無限に

レベル1は 4×4 に 1 色 2 個、PAR 1手。ここから少しずつ広く・多色に・意地悪になっていきます。

| 変わるもの | 進み方 |
|---|---|
| **色数** | 2 レベルごとに 1 色ずつ増え、**レベル21で 11 色（上限）** |
| **ブロック数** | 色数 × 2。レベル21以降は 22 個 |
| **盤面** | 色数から決まる（1 色あたり 8 マス、目標の埋め率 62%）。レベル1で 4×4、**レベル21で 12×12（上限）** |
| **仕込み手** | レベル13から 1 手ずつ増え、**レベル28で 4 手（上限）** |
| **一本道** | **レベル16から**、どの局面でも「消せる手」が実質 1 通りしかない盤面を狙う |

ブロックは **テトロミノ（4マス）だけ**。難しさは形ではなく、色数・仕込み手・一本道でつけています。

### 仕込み手 ― 一見関係ないところを動かす

**それ自体では何も消えない手**のこと。通さないと後の手が成立しないので、「なぜここを動かすのか」が直前には分からない。生成器は仕込み手を入れるとき、**その手を通すまで盤面に消せる手がひとつも無い**配置を優先して選びます（実測でレベル13以降の 52/68 で達成）。盤面を眺めても最初の一手が見つからないとき、答えは「消えない手」です。

### 一本道 ― 解が実質 1 通り

同じ色が 2 個ずつしか無いので、消去は必ず 1 色ぶんを取り除くだけ。つまり **結果の盤面は「どの色を消したか」だけで決まります**。したがって「消せる色が 1 つしかない ＝ 次の一手が実質 1 通り」。生成器はレベル16以降、各ステップでこの条件を満たす配置を探します（実測で 60/65 で達成）。

`npm run verify` で測ったレベル 1〜80 の実測値：

| レベル | 盤面 | 色 | 個数 | 埋め率 | PAR | 仕込み | 一本道 |
|---|---|---|---|---|---|---|---|
| 1 | 4×4 | 1 | 2 | 50% | 1 | – | – |
| 5 | 6×6 | 3 | 6 | 67% | 3 | – | – |
| 10 | 8×8 | 5 | 10 | 63% | 5 | – | – |
| 15 | 10×10 | 8 | 16 | 64% | 9 | 1手 | – |
| 20 | 11×11 | 10 | 20 | 66% | 12 | 2手 | ✓ |
| 25 | 12×12 | 11 | 22 | 61% | 14 | 3手 | ✓ |
| 30〜 | 12×12 | 11 | 22 | 61% | 15 | 4手 | ✓ |

**レベル番号 → シード → 決定論的な生成** という一本道なので、**同じレベルなら、どの端末でも必ず同じ盤面**です。URL のハッシュ（`#L26`）でレベルを直接開けます（未開放のレベルはレベル一覧に案内されます）。

> **色数の上限が 11 なのはなぜか。** 色そのものは何色でも作れます（`colorFor(i)` が番号から手続き的に作る）。ただし 11 色 = 22 ブロック = 88 マスで、最大盤面 12×12 の 61%。これ以上詰めると滑走路が足りなくなり、**一本道の成立率が急落します**（12色で 4/8、14色では 1/8 まで落ちる）。盤面の上限 12×12 のもとでは、11 色が「一本道と仕込み手が両立する」いちばん濃い点でした。

---

## 見た目と手触り

**UI は退き、ブロックの色だけが前に出る。** iOS 26 の Apple Music に倣った作りにしている。

- **明るい配色ひとつだけ** ― ダークモードは持たない。端末の設定に関わらず、ブロックの色が同じに見えることを優先している
- **大きなタイトル** ― 左上に「レベル 24」を 34px / ウェイト800 / トラッキング -0.03em で置き、その下に薄いグレーの補足（`12×12・11色・仕込み3手・一本道`）。書体は SF（`-apple-system`）、日本語はヒラギノ角ゴ
- **下に浮くガラスのツールバー** ― 戻す／ヒント／やり直すのピルと、レベル一覧の**独立した丸ボタン**。位置は `fixed` で、盤面はその下を通り、ガラス越しにぼけて透ける
- **カプセル型のセグメント** ― 手数・PAR・残りは丸いカプセルに。色チップも同じ言語
- **設定は iOS のリスト** ― 角丸のグループ、文字の頭から始まる細いインセット区切り線、iOS 風トグル。モーダルは下から出るシート（グラバー付き、広い画面では中央のカード）

### ブロックは色そのもの

**影も光沢も模様も乗せない。** 一色のベタ塗りに、ほんのわずかな角丸だけ。

- **すき間は 1〜2px** ― マスの大きさに関係なく髪の毛ほどしか空けないので、盤面はタイルを敷き詰めたモザイクのように見える
- **同じブロックのマス同士は継ぎ目なく繋がる** ― すき間が入るのは別のブロックとのあいだだけ。だから「どこまでが一緒に動くか」は形で読める
- **後ろの盤面も同じ考え** ― 淡い色のマスを、ブロックと同じ寸法・同じすき間で敷き詰めただけの平らな面。空きマス（＝通路）がそのまま読める
- **色は無制限** ― 手で選んだ一覧ではなく、「隣り合う番号どうしがいちばん離れて見える色相の並び」から手続き的に作る。一覧を使い切ったら色相をずらし、明度と彩度も周ごとに変えるので、何色でも別の色として読める

### 消える瞬間を祝う

| 行為 | 反応 |
|---|---|
| ブロックを掴む | 小さなティック音 |
| 滑って壁にぶつかる | マリンバのような丸い着地音（距離が長いほど低い）＋ 進行方向へのつぶれ |
| 消える | **音程が連鎖ごとに階段状に上がる**破裂音（ペンタトニック）＋ 破片が飛び散る ＋ 白いフラッシュ ＋ 拡散リング ＋ 画面の揺れ |
| 大量消去 | 「○個消し！」の浮き文字＋段階評価 ＋ 連鎖数 |
| クリア | 星が 1 つずつ跳ねて出る |
| 動かせない方向 | ブロックが小刻みに揺れて拒否＋低い短音（手数には数えない） |

効果音は音源ファイルを持たず、WebAudio でその場で合成している（`src/audio.js`）。iOS は最初のタップまで音を出せないので、最初のポインタ操作で解錠している。

---

### 面白さの正体

消去は盤面のトポロジーそのものを書き換える。1 色消えることで届かなかった場所へ届くようになり、止まっていた位置が意味を失う。**「どの順番で消すか」が「何を消せるか」を決める。**

同じ色が 2 個ずつしか無いという設計は、この循環を極端に純粋にする。**盤面の状態は「まだ消えていない色の集合 ＋ 位置」でしかなく、1 手の結果は「どの色を消したか」だけで決まる。** だから「消せる色が 1 つだけ」という条件を課せば、それがそのまま **一本道** になる。上のレベルの手応えは、この一点から生まれている。

---

## 「解ける盤面」を構造的に保証する

ランダムに埋めて解けるか検証する、という素朴な方法は使っていない。代わりに **「解の逆順構築」** を採用している。

**盤面を作って解くのではなく、解を作って盤面を導く。** 完成状態（空盤面）から出発し、1 手ぶんずつ逆再生するようにブロックを置いていく。最後に置いたステップが、解の第 1 手になる。

```
S_N = ∅（空盤面）← ステップ1 ─ S_{N-1} ← ステップ2 ─ … ← ステップN ─ S_0（初期盤面）
```

各ステップで置くもの ―― **色 c を新しく 1 つ選び、テトロミノを 2 個置く**：

- **移動ブロック P**：着地点 B・滑走方向 d・開始点 A (= B − t×d)
- **相棒ブロック Q**：同じ色 c。B に居る P に隣接する

配置時に課す制約と、それが保証する性質：

| 制約 | 保証する性質 |
|---|---|
| 新規セルはすべて空きマス | ブロックの重複なし |
| A→B の経路が空、B+d に壁かブロック | その手が実際に実行可能で、ちょうど B で止まる |
| A に居る P は Q に触れていない | 初期盤面から同色は隣接しない |

色ごとにブロックは 2 個しかないので、**「予定外の巻き込み消去」は原理的に起きません**。

### 仕込み手と一本道を後から足す

- **仕込み手**：盤面にあるブロックを 1 つ選び、「滑らせれば今の位置にちょうど来る」手前の位置へ戻す。候補を列挙して**動かした結果いちばん消せる色が減るもの**（0 になれば必須手）を選ぶ
- **一本道**：ペアを置いた直後に「消せる色がこの色だけか」を確かめ、そうでなければ置き直す。判定は 2 色見つかった時点で打ち切り、試行回数にも上限を置く。上限まで見つからなければ、最初に見つかった普通の配置に落とす ―― **一本道は「狙う」ものであって、生成そのものを失敗させない**

### なぜ不変条件が効くか

どの手の直後も「同色ブロック同士は隣接していない」状態が保たれる（触れた瞬間に消えるため）。この不変条件があるからこそ **「動かしたブロックが触れた相手だけが消える」** ことが確定し、逆順構築の各ステップを独立に検証できる。副産物として、**PAR（保証解の手数）とヒント機能がほぼ無償で手に入る** ―― 解そのものがデータとして存在するからだ。

`npm run verify` はレベル 1〜80 をすべて生成し、解答手順を実際に実行して本当に全消しできることを確かめます：

```
失敗            : 0
埋め率          : 平均 61.2%
PAR             : 平均 12.7 (1〜15)
一本道の達成    : 60/65
仕込みが必須    : 52/68
生成時間        : 平均 779ms / 最大 1.7s
```

上のレベルは生成に 1 秒を超えることがあるので、ゲーム側は**非同期版の生成器**を使い、試行の合間にイベントループへ制御を返しながら「組み立て中」を表示します。画面は固まりません。

---

## 操作

| 操作 | 動作 |
|---|---|
| ブロックをスワイプ | その方向へスライド（26px の閾値を超えた瞬間に発動） |
| ドラッグ中 | **着地予測ゴースト**と矢印を表示。消える相手は黄色く光る |
| クリック → 矢印キー | 精密操作・キーボード派向け |
| `Z` / `H` / `R` / `L` / `Esc` | 戻す / ヒント / やり直す / レベル一覧 / ホーム |

動かせない方向へスワイプするとブロックが小刻みに揺れて拒否する（手数には数えない）。

盤面上部の色チップは色ごとの残りブロック数で、残り 1 個の色（＝相棒がいない色）は縁取られる。

---

## 構成

```
index.html          3 つの画面（app.js?v=<ハッシュ> を読む）
styles.css          UI スタイル（ガラス・シート・レベル一覧）
app.js              tools/build.mjs が src/ から生成する配信物。直接編集しない
src/
  rng.js            シード付き乱数（mulberry32）
  shapes.js         テトロミノ7種の全回転（19 向き）
  levels.js         レベル定義（色数・盤面サイズ・仕込み手・一本道・シード）
  board.js          盤面モデルとルール（スライド / 同色グループ / 消去）
  generator.js      解の逆順構築によるパズル生成 + 解答手順の検証
  render.js         Canvas 描画（無制限の色・平らなブロック・ゴースト・粒子）
  audio.js          WebAudio で合成する効果音（音源ファイル不要）
  input.js          スワイプとクリック選択
  game.js           進行・アニメーション・Undo・ヒント・画面遷移・星と開放
  main.js           起動と URL ハッシュの解釈
tests/              ルール・生成器・レベル進行・配信物のテスト（node --test）
tools/build.mjs     src/ を app.js に束ねてハッシュを刻む
tools/verify.mjs    レベルの大量検証と統計
server.mjs          依存ゼロの静的サーバ
```

ルールの実装（`src/board.js`）とパズル生成（`src/generator.js`）はブラウザ API に一切依存していないので、Node からそのままテストできる。

### 実装上のメモ

- **敗北条件を持たない。** 以前は「2 手先まで見て消せる手が無ければ詰み」としていたが、同じ色が 2 個ずつという設計では消せる手が見当たる局面のほうが少ない（仕込み手が必須の盤面では初手からゼロ）。詰み判定そのものを外し、`戻す` と `やり直す` に任せている。
- **「消せる色」は 2 色見つけたら数え終える。** 一本道の判定は「消せる色が 1 つだけか」なので、全部数える必要はない。上限付きで打ち切るようにして、レベル23の生成が 7.2 秒から 1.4 秒になった。
- **ヒントは 2 段構え。** 現在の局面が保証解の道筋上にあれば（局面の指紋で照合）その手を、外れていれば「いま最も多く消せる手」を示す。仕込み手のときは「これ自体は何も消えません」と伝える。
- **埋め率は詰め込み優先度で稼ぐ。** 逆順構築で開始点 A を選ぶとき、壁や既存ブロックに接している配置から順に試すことで空きマスの断片化を防いでいる。
- **ブロックは外周だけを縁取る。** セル矩形の集合をそのまま stroke すると内部のセル境界にも線が出て、1 個のブロックが格子模様に見えてしまう。外周の辺と外側の角だけを集めた Path2D を別に作って使っている。
- **配信物は 1 本に束ねる。** `index.html` と `src/*.js` を別々に配信すると、ブラウザが「新しい HTML ＋ 古い JS」を組み合わせてしまうことがある（GitHub Pages はどちらも短い max-age で配信し、Safari は JS を長く保持する）。実際にそれで盤面が真っ白になった。`app.js?v=<内容ハッシュ>` に束ねれば HTML と JS が食い違いようがなく、`file://` でも動く。生成物が古いままにならないよう、`npm test` が最新かどうかを検査する。
- **`roundRect` は自前でも用意する。** Safari 16.4 未満に無いので、無ければ `CanvasRenderingContext2D` と `Path2D` に足している。

---

## 位置づけ

| ゲーム | 移動 | 消去 | 重力 |
|---|---|---|---|
| テトリス | 落下・回転 | ライン揃え | あり |
| ぷよぷよ | 落下 | 同色4連結 | あり（連鎖） |
| SameGame | クリック | 同色連結 | あり |
| Rush Hour | スライド | なし | なし |
| アイススライド系 | 決定論的スライド | なし | なし |
| **SLIDE POP!** | **決定論的スライド** | **同色ペア接触** | **なし** |

「消去が通路を生み、通路が消去を可能にする」という循環構造は、重力型パズルにも純スライドパズルにも存在しない。

詳しい設計思想は [docs/GAME_DESIGN.md](docs/GAME_DESIGN.md) を参照。

## ライセンス

MIT
